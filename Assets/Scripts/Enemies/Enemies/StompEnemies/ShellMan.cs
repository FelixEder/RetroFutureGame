using UnityEngine;
using System.Collections;

public class ShellMan : MonoBehaviour {
	public bool deShelled;
	/*
	 * Has shell that can be removed by some kind of attack (probably stomp)
	 * Should jump.
	 * Is pretty much exactly the same as crawlercritter, should it use same script maybe?
	 */
}